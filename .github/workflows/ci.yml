name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Build
        run: xcodebuild -project mdql.xcodeproj -scheme mdql -destination 'platform=macOS' build

      - name: Test
        run: xcodebuild -project mdql.xcodeproj -scheme mdql -destination 'platform=macOS' test
